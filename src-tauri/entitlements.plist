<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened-runtime entitlements for the Developer ID (non-sandboxed) build.

    This app is NOT sandboxed: it reads ~/.claude and ~/.zai directly, which
    the App Store sandbox would forbid. All outbound network calls (z.ai,
    Anthropic) are made from the Rust process; the WKWebView runs its
    JavaScript in a separate system process, so no JIT/network entitlements
    are required here.

    allow-jit is declared defensively for the in-process JS bridge and is
    notarization-safe. Add more entries only if notarization specifically
    rejects the binary.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
</dict>
</plist>
